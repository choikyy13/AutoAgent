# requirements.txt
# ----------------
# Minimal dependencies for Track-2 pipeline

pypdf                 # Extract text from PDFs
requests              # Download PDF files
beautifulsoup4        # Optional: HTML parsing
python-dotenv         # Load API keys
##openai                # LLM API (can swap with Gemini or Constructor)
GitPython           # Clone GitHub repositories
jsonschema            # Validate JSON structures
pdfminer.six         # Alternative PDF text extraction